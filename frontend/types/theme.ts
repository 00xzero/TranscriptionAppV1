// User's theme *preference*. 'system' follows the OS via prefers-color-scheme;
// 'light'/'dark' pin the applied theme regardless of the OS setting.
export type AppTheme = 'light' | 'dark' | 'system'
